{
  "name": "ministry-of-minutes",
  "version": "1.0.0",
  "description": "Secretariat workflow automation for Singapore Government meetings",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "dev": "concurrently \"node server/index.js\" \"npm run dev --prefix client\"",
    "build": "npm run build --prefix client",
    "package": "npm run build --prefix client && cp -r client/dist server/public && zip -r ministry-of-minutes.zip . --exclude 'node_modules/*' 'client/node_modules/*' '.env' '.git/*'"
  },
  "dependencies": {
    "anthropic": "^0.40.0",
    "better-sqlite3": "^9.4.3",
    "cors": "^2.8.5",
    "docx": "^8.5.0",
    "dotenv": "^16.4.5",
    "express": "^4.18.3",
    "multer": "^1.4.5-lts.1",
    "nodemailer": "^6.9.13"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18"
  }
}
